# === Backend / FastAPI ===
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
cryptography>=42.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
PyJWT>=2.8.0

# === Database (SQLAlchemy + asyncpg for FastAPI, psycopg2 for PGVector) ===
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# === AI / LangGraph / RAG ===
langchain>=0.2.0
langgraph>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-google-genai>=1.0.0
langchain-postgres>=0.0.3
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0
pgvector>=0.2.5
openai>=1.20.0

# === LangGraph Checkpoint (psycopg3 async driver) ===
psycopg[binary,pool]>=3.1.0
langgraph-checkpoint-postgres>=0.0.1

# === Embeddings & ML ===
sentence-transformers>=3.0.0
# Pinned <2: NumPy 2.x manylinux wheels require the x86-64-v2 CPU baseline, which the
# Proxmox deploy VM (generic kvm64/qemu64 vCPU) does not expose → RuntimeError on import.
# 1.26.x wheels use the x86-64-v1 baseline and run there. (Permanent fix: set the VM CPU
# type to `host` in Proxmox, then this pin can be relaxed.)
numpy>=1.26.0,<2.0

# === Celery (background tasks, scheduling, monitoring) ===
celery[redis]>=5.3.0
flower>=2.0.0

# === Redis ===
redis>=5.0.0

# === HTTP ===
httpx>=0.27.0

# === SOAP (real I-Way Axis2 web services) ===
zeep>=4.2.1

# === Auth ===
bcrypt>=4.0.1

# === Utilities ===
typing_extensions>=4.9.0

# === Semantic Caching ===
redisvl>=0.3.0

# === Observability / OpenTelemetry ===
opentelemetry-distro>=0.45b0
opentelemetry-exporter-otlp>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-instrumentation-psycopg>=0.45b0
opentelemetry-instrumentation-redis>=0.45b0
