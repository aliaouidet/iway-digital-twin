{
  "name": "iway-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end (Playwright) tests for the I-Way Digital Twin — run against the mock-mode Docker stack.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
